services:
  api_version:
    build:
      context: .
      dockerfile: dockerfile.prod
    image: drglasdou/gql_template:${APP_VERSION:-1.0.0}

  api_latest:
    build:
      context: .
      dockerfile: dockerfile.prod
    image: drglasdou/gql_template:latest
