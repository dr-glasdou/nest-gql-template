services:
  api_version:
    build:
      context: .
      dockerfile: dockerfile.prod
    image: drglasdou/nest_gql:${APP_VERSION:-1.0.0}

  api_latest:
    build:
      context: .
      dockerfile: dockerfile.prod
    image: drglasdou/nest_gql:latest
